<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.qmsos.quakemo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".EarthquakeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EarthquakePreferences" >
        </activity>
        <activity
            android:name=".EarthquakeSearchResults"
            android:label="Earthquake Search"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <meta-data
            android:name="android.app.default_searchable"
            android:value=".EarthquakeSearchResults" />

        <provider
            android:name=".EarthquakeProvider"
            android:authorities="org.qmsos.earthquakeprovider"
            android:exported="false" >
        </provider>

        <service
            android:name=".EarthquakeUpdateService"
            android:enabled="true" >
        </service>
        <service
            android:name=".EarthquakeRemoteViewService"
            android:permission="android.permission.BIND_REMOTEVIEWS" >
        </service>

        <receiver
            android:name=".EarthquakeAlarmReceiver"
            android:exported="false" >
            <intent-filter>
                <action android:name="org.qmsos.quakemo.ACTION_REFRESH_EARTHQUAKE_ALARM" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".EarthquakeWidget"
            android:label="@string/widget_name" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="org.qmsos.quakemo.QUAKES_REFRESHED" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/earthquake_widget_info" />
        </receiver>
        <receiver
            android:name=".EarthquakeListWidget"
            android:label="@string/widget_list_name" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/earthquake_list_widget_info" />
        </receiver>
    </application>

</manifest>